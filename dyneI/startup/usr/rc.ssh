#!/bin/sh

FILE_ID="\$Id$"
source /lib/libdyne.sh

notice "launching ssh daemon"
if [ ! -e /home/.ssh/identity ]; then
    act "generating ssh identity key in background"
    (ssh-keygen -t dsa -q -f /home/.ssh/identity -N ""; sshd) &
else
    sshd &
fi
