#!/bin/sh

if [ -z "$1" ]; then
  TITLE="Terminal"
else 
  TITLE="$1"
  shift
fi

if [ -z "$1" ]; then
  PROG="`which zsh`"
else
  PROG=$@
fi

launch \
mrxvt -tn xterm-color -bg black -fg white -T "$TITLE" \
      -tabfg lightgrey -itabfg darkgrey -tr -trm -trt -ss rxvt \
      -sr -bgfade 80 -fade 50 -bc -vb -e $PROG

